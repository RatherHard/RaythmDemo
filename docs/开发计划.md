# RaythmDemo 开发计划

本文档根据 `docs/开发日志.md` 汇总过去计划、当前状态与短期开发计划。状态标记说明：`[x]` 已完成，`[ ]` 未完成或待推进。

---

## 计划总览

### 已完成计划

- [x] 初始化项目协作规范与技术基线。
- [x] 明确核心技术选型：C++20、CMake/CTest、miniaudio、Vulkan/Volk、SDL3、nlohmann/json。
- [x] 建立标准化项目目录结构与文档规范。
- [x] 配置 CMake、vcpkg 与 VSCode 开发环境。
- [x] 接入基础环境测试 `RaythmDemoEnvironmentTest`。
- [x] 建立 SDL3 窗口类基础封装。
- [x] 接入窗口测试 `RaythmDemoWindowTest`。
- [x] 同步 `.clangd` 基础 include 路径和平台宏。
- [x] 将 Platform 模块代码迁移到 `Raythm::Platform` 命名空间。
- [x] 建立开发日志维护规则。
- [x] 建立开发前阅读开发日志的上下文检查规则。
- [x] 建立 Render 模块 Vulkan 清屏与 Present 骨架，并接入 `RaythmDemoRenderTest`。
- [x] 建立 Core 模块基础设施、`Core::Application`、时间系统和 `RaythmDemoCoreTest`。
- [x] 建立 Audio 最小播放与音频时钟骨架、`AudioEngine` 和 `RaythmDemoAudioTest`。
- [x] 建立 Render 纯策略测试、最小 2D colored rect 绘制入口和 `RaythmDemoRenderStrategyTest`。
- [x] 新增 `.mc` 谱面转换器，将 Malody 风格谱面导入为 RaythmDemo 当前 JSON 谱面格式，并接入 `RaythmDemoMcConverterPyTest`。
- [x] 建立 `GameplaySession` 纯 Game 判定会话，基于谱面 beat/BPM、`meta.offset`、Audio playback time 语义时间与 `LaneInputSnapshot` 推进 tap note 判定、miss、combo 和 `GameplaySnapshot`。
- [x] 建立最小 Core 运行时游玩闭环，启动时加载 `assets/charts/00001/00001.json` 与 `00001.ogg`，每帧用 Audio playback time、`InputState`、`GameplaySession` 和 `GameplayViewAdapter` 生成并提交 `RenderCommand`。
- [x] 修复启动谱面 Ogg Vorbis 音频加载失败，vendored `stb_vorbis` 并通过 miniaudio Vorbis decoder、AudioEngine 内存加载和真实启动 smoke 验证。
- [x] 修复启动窗口消息泵缺失导致的 Windows 未响应问题，完成最小可玩切片真实运行验证。

### 短期计划

- [x] 建立 Platform 薄 SDL 事件泵并完成窗口事件处理职责收缩。
- [x] 扩展 Platform 模块输入事件封装，继续补齐键盘、鼠标按键、鼠标移动等设备输入事件边界。
- [x] 建立输入状态系统，覆盖键盘、鼠标按键、鼠标移动与按下/释放边沿状态，为后续音游低延迟输入打底。
- [x] 优先建立 Core 模块基础设施，包括 `Core::Application`、主循环、时间系统和应用生命周期管理，将当前 `main.cpp` 中的窗口、事件泵、输入状态与渲染帧循环收束到 Core 层。
- [x] 建立 Core 时间系统，提供 monotonic time、delta time 与 frame counter，为后续音游判定窗口、音频同步和谱面滚动打底。
- [x] 明确 Core、Platform、Audio、Render、Game 模块边界和初始化顺序，固化 `Core` 统筹生命周期、`Platform` 提供窗口/事件/输入、`Render` 依赖 Vulkan-capable window/surface 的接入关系。
- [x] 优先建立 Audio 最小播放与音频时钟骨架，提供 miniaudio 生命周期封装、音乐播放状态和 playback time，作为 Game 判定与谱面滚动的主时钟基础。
- [x] 继续拆分 Render 模块纯选择策略测试，例如 surface format、present mode、swapchain extent 与 queue family 选择，降低 GPU 环境依赖。
- [x] 建立 Render 最小 2D 绘制能力，至少支持轨道、判定线和 tap note 的 colored rect 绘制入口。
- [x] 在已具备 Audio 时钟、Render 2D、谱面加载、输入映射与视图适配 seam 的基础上，完成 Game 最小可玩切片的纯逻辑判定闭环：tap note 判定窗口、Audio playback time 语义时间推进、miss/combo 统计和只读 `GameplaySnapshot` 输出。
- [x] 在 Game 纯逻辑判定闭环基础上，继续推进最小 Core 接入：运行时加载谱面、读取 Audio playback time、采样 `InputState`、更新 GameplaySession，并提交 RenderCommand。
- [x] 在 Game 最小可玩切片中实现谱面文件解析和加载，将 `docs/玩法设计文档/谱面文件格式定义.md` 中的 `meta`、`time`、`note` 转换为可测试的运行时 `Chart` 数据。
- [x] 建立语义输入映射层 `InputState -> LaneInputSnapshot`，将 Platform 物理输入状态转换为 Game 可消费的轨道输入快照。
- [x] 建立玩法视图适配层 `GameplaySnapshot -> RenderCommand`，将 Game 运行时快照转换为 Render 最小 2D 绘制命令。
- [x] 建立 `.mc` 谱面导入工具，将 `assets/charts/00001/1627802685.mc` 这类 Malody 风格谱面转换为 RaythmDemo 当前 JSON 谱面格式。
- [ ] 在 Game 最小运行闭环跑通后，继续完善跨平台构建验证，至少补齐 Linux 下 SDL3、Volk、Vulkan loader、pthread 与 dl 动态链接相关配置核对。

### 当前进度摘要与下一步建议

**当前状态**

- Platform/Core/Audio/Render 的底层能力已基本打通：窗口与事件泵、输入状态、应用主循环、音频播放时钟、Vulkan Present 与最小 2D colored rect 绘制均已完成并接入 CTest。
- Game 层已经完成最小可玩切片的前置 seam 与纯逻辑判定闭环：`ChartLoader` 可加载 RaythmDemo JSON 谱面，`LaneInputMapper` 可将 `InputState` 映射为 4K 轨道输入，`GameplaySession` 可基于 beat/BPM、`meta.offset`、playback time 语义时间和 `LaneInputSnapshot`/timestamped `LanePressEvent` 推进 tap 判定、miss、combo 与 `GameplaySnapshot`，`GameplayViewAdapter` 可将 `GameplaySnapshot` 转换为 `RenderCommand`。
- `.mc` 谱面转换器已完成首版，可将 Malody 风格 `.mc` 谱面转换为当前 RaythmDemo JSON 谱面格式，并已通过真实样例、CLI 和错误输入测试。
- 最小 Core 运行时游玩闭环已完成真实运行验证：`Core::Application` 启动时加载可执行文件旁 staged `assets/` 中的 `charts/00001/00001.json` 与 Ogg 音频，使用 `AudioEngine` playback time 驱动 `GameplayRuntimeDriver`，每帧显式 `SDL_PumpEvents()` 后消费 `EventPump`，并向 `Renderer` 提交玩法 2D commands。
- Ogg Vorbis 启动音频加载失败已修复：miniaudio implementation translation unit 已通过 vendored `stb_vorbis` 启用 Vorbis backend，`RaythmDemoEnvironmentTest` 覆盖项目 Ogg 资产解码，`RaythmDemoAudioTest` 覆盖启动同类内存音频加载 seam。
- Windows 窗口未响应问题已修复：`Platform::EventPump` 使用 `SDL_PeepEvents()`，因此 Core 主循环已在每帧事件读取前显式调用 `SDL_PumpEvents()`，人工验证窗口点击不再提示未响应，音乐、谱面滚动与 D/F/J/K 输入可交互。

**下一步优先级**

1. **完善运行时产品入口与状态表达**：最小可玩切片已完成真实运行验证，下一步优先补齐暂停/恢复、重新开始、选曲或配置入口，并新增独立于 `ApplicationState` 的 runtime state/health 表达。
2. **完善判定与计分语义**：继续补完整 score 公式、hold sustain/release 判定、`visualOffset` / `audioOffset` 配置语义和结果界面所需统计。
3. **明确 offset 与校准语义**：确认 RaythmDemo `meta.offset` 与 Malody `.mc` offset 的符号和语义，并规划 raw decoder/source cursor、实际可听输出时间、`audioOffset` 与 `visualOffset` 的校准边界。
4. **随后做 Linux 验证与稳定性增强**：集中验证 Linux 下 CMake/CTest、SDL3、Volk、Vulkan loader、pthread/dl 链接，以及后续 Vulkan device lost、等待超时和音频延迟校准问题。

**下一步推进计划：运行时闭环复测、卡死/同步修复与玩法完善**

- [x] 设计并实现 `Core::GameplayRuntimeDriver`，保持 Core 负责生命周期编排，Game 仍只暴露纯逻辑 `GameplaySession`，Render 仍只消费 `RenderCommand`。
- [x] 确定首个运行时谱面资产策略：使用已提交的 `assets/charts/00001/00001.json` 与 `00001.ogg`，运行时不直接读取 `.mc`。
- [x] 在 Core 初始化阶段创建并持有 `AudioEngine`、`Chart`、`GameplayRuntimeDriver`、`LaneInputMapper`、`GameplaySession` 与 `GameplayViewAdapter` 所需状态，并按现有 RAII 顺序保证 Audio/Game 资源早于窗口和 Renderer 安全释放。
- [x] 在主循环中按固定顺序执行：读取 `AudioEngine::getPlaybackTimeSeconds()`、采样 `InputState`、转换 `LaneInputSnapshot`/timestamped `LanePressEvent`、调用 `GameplaySession::update()`、获取 `GameplaySnapshot`、生成并提交 `RenderCommand`、最后渲染帧。
- [x] 为运行时闭环补充可测试 seam，使用合成 chart、显式 song time 和 `InputState` 覆盖 Core/Game 驱动顺序，真实音频、窗口和 Vulkan 路径保留 smoke/manual 验证。
- [x] 保持 `cmake --build build --config Debug` 与 `ctest --test-dir build -C Debug --output-on-failure` 全绿，并通过 CMake POST_BUILD 将 `assets/` staged 到可执行文件目录。
- [x] 修复并验证 `charts/00001/00001.ogg` 启动音频加载：`RaythmDemoEnvironmentTest` 可直接解码项目 Ogg Vorbis 资产，`RaythmDemoAudioTest` 可通过 `AudioEngine::loadMusicFromMemory()` 加载真实 Ogg bytes，`timeout 5s ./build/Debug/RaythmDemo.exe` 不再复现 `unable to load music track`。
- [x] 建立运行时卡死与音频/谱面不同步的诊断反馈环：已通过 `RaythmDemoCoreGameplayRuntimeTest` 覆盖 input timestamp 到 song time 映射，通过 `RaythmDemoRenderStrategyTest` 覆盖 Vulkan fence/acquire/present result 分类，并记录 runtime smoke 行为。
- [x] 将 Render 主线程路径中的无限等待改为有限等待与可观测错误/恢复状态，覆盖 fence wait、swapchain image acquire、swapchain recreate 和 shutdown wait，使渲染 stall 时 Core 能继续泵 SDL 事件并响应关闭。
- [x] 修复 Windows 窗口未响应问题：由于 `Platform::EventPump` 使用 `SDL_PeepEvents()`，Core 主循环已在每帧读取事件前显式调用 `SDL_PumpEvents()`，保证系统窗口消息持续进入 SDL 事件队列。
- [x] 在 Vulkan-capable SDL 环境下手动复测 `build/Debug/RaythmDemo.exe`，确认窗口打开、音乐播放、note 滚动、D/F/J/K 视觉反馈和判定交互符合当前最小切片预期。
- [ ] 在 Core 中新增独立于 `ApplicationState` 的运行时状态机，明确 `LoadingAssets`、`Ready`、`Playing`、`SuspendedNoDrawable`、`RenderStalled`、`RecoveringSwapchain`、`SongEnded` 与 `FatalError` 等状态下 Audio、Gameplay、Input、Render 的推进/暂停规则。
- [x] 为 `Platform::InputEvent` 保留事件发生时间戳，并在 Core 中建立事件时间到 song time 的映射，避免用“事件被处理时”的 `AudioEngine::getPlaybackTimeSeconds()` 作为判定时间。
- [ ] 明确并测试 RaythmDemo `meta.offset` 与 Malody `.mc` offset 的符号和语义，必要时修正转换器或运行时 offset 应用，并用已知对齐点补充回归测试。
- [ ] 引入音频/视觉校准计划：区分 raw decoder/source cursor、实际可听输出时间、`audioOffset` 与 `visualOffset`，避免后续只靠判定窗口掩盖固定延迟。
- [ ] 在 Vulkan-capable SDL 运行环境下手动复测 `build/Debug/RaythmDemo.exe`，确认窗口打开、音乐播放、note 滚动、D/F/J/K 视觉反馈和判定消除效果。
- [ ] 继续实现完整 score 公式、hold sustain/release 判定、`visualOffset` / `audioOffset` 配置语义和结果界面统计。

---

## 阶段计划

### 阶段 0：项目初始化与规范建立

**状态**：`[x]` 已完成

**目标**

- 建立项目协作规范、技术选型文档和目录结构。
- 明确跨平台 C++20 音乐节奏游戏的基础约束。

**完成情况**

- [x] 创建 `CLAUDE.md`，记录项目概述、技术栈、目录结构、编码规范、测试规范和提交规范。
- [x] 创建 `docs/技术设计文档/核心技术选型.md`，固化核心技术方案。
- [x] 建立 `docs/savedprompt/local/环境配置与测试/` 提示词存档。

**验证依据**

- `docs/开发日志.md` 中 `2026-07-02 初始化项目文档与系统规范`。

---

### 阶段 1：构建环境与依赖验证

**状态**：`[x]` 已完成

**目标**

- 建立可配置、可构建、可测试的 CMake/vcpkg/VSCode 工程基础。
- 验证核心第三方依赖能被项目发现和链接。

**完成情况**

- [x] 创建顶层 `CMakeLists.txt` 并启用 C++20 与 CTest。
- [x] 创建 `vcpkg.json`，声明 `sdl3`、`nlohmann-json`、`volk`。
- [x] 创建 VSCode 任务、调试、设置和扩展建议配置。
- [x] 添加 `tests/environment_test.cpp` 和 `src/miniaudio_impl.cpp`。
- [x] 注册并维护 `RaythmDemoEnvironmentTest`。

**验证依据**

- `docs/开发日志.md` 中 `2026-07-03 配置 CMake、vcpkg 与 VSCode 开发环境`。
- 当前 Debug CTest 已通过 `RaythmDemoEnvironmentTest`。

---

### 阶段 2：Platform 窗口基础设施

**状态**：`[x]` 已完成基础封装，`[x]` 输入封装待推进

**目标**

- 建立 SDL3 窗口 RAII 封装。
- 提供后续 Vulkan Surface 创建和输入系统接入所需的平台窗口能力。

**完成情况**

- [x] 创建 `include/Platform/Window.hpp` 和 `src/Platform/Window.cpp`。
- [x] 支持窗口创建、销毁、移动语义、尺寸查询、显示/隐藏、无边框全屏切换。
- [x] 支持窗口事件过滤和基础窗口事件翻译。
- [x] 支持 `SDL_EVENT_QUIT` 转换为应用级退出请求，避免平台退出信号被主循环忽略。
- [x] 提供 `getRequiredVulkanInstanceExtensions()` 和 `createVulkanSurface(VkInstance)`。
- [x] 添加并注册 `RaythmDemoWindowTest`。
- [x] 将窗口类型迁移到 `Raythm::Platform` 命名空间。

**短期待办**

- [x] 新增轻量 `Platform::EventPump`，作为窗口事件的 SDL 全局事件队列消费者。
- [x] 新增项目自有输入事件类型，覆盖键盘、鼠标按键与鼠标移动，避免将 SDL 原始事件暴露到 Core、Render、Game 层。
- [x] 收缩 `Window` 的事件职责，使其主要负责 SDL 窗口资源、窗口 ID、尺寸状态、Vulkan surface 创建与窗口状态查询。
- [x] 建立输入状态系统，先覆盖键盘、鼠标按键与鼠标移动，并保留输入事件时间戳扩展空间。

**验证依据**

- `cmake --build /e/RaythmDemo/build --config Debug` 构建通过。
- `ctest --test-dir /e/RaythmDemo/build -C Debug --output-on-failure` 执行通过，2/2 tests passed。
- `docs/开发日志.md` 中 `2026-07-04 建立基础设施测试与 SDL3 窗口封装` 与 `2026-07-04 规范化模块命名空间`。

---

### 阶段 3：Render Vulkan 清屏与 Present 骨架

**状态**：`[x]` 已完成最小清屏 Present 骨架；纯策略测试与最小 2D 绘制能力已在阶段 7 完成

**目标**

- 建立 `Raythm::Render` 命名空间下的 Vulkan 初始化基础，为后续谱面渲染和低延迟画面同步打底。
- 让 SDL Vulkan 窗口能够完成稳定清屏、Present、生命周期管理和 resize/swapchain 重建。

**完成情况**

- [x] 新增 `include/Render/Renderer.hpp` 与 `src/Render/Renderer.cpp`。
- [x] 建立 `Raythm::Render::Renderer` RAII 封装，集中管理 Vulkan instance、surface、physical/logical device、swapchain、image view、command buffer 与同步对象。
- [x] 按规范完成 `volkInitialize()`、`VkInstance` 创建、`volkLoadInstance(instance)`、SDL `VkSurfaceKHR` 创建与 `volkLoadDevice(device)`。
- [x] 支持物理设备、graphics/present queue family、逻辑设备与 swapchain 的最小选择流程。
- [x] 使用 `vkCmdClearColorImage` 完成 swapchain image 清屏，并提交到 present queue。
- [x] 处理 resize、pixel-size changed、restore、minimize、out-of-date 与 suboptimal present 的 swapchain 生命周期。
- [x] 将 `RaythmDemo` 主入口接入最小 SDL/Vulkan 事件循环。
- [x] 添加并注册 `RaythmDemoRenderTest`，验证 renderer 构造、清屏 Present 与 resize 事件路径。

**短期待办**

- [x] 拆分可无 GPU 测试的纯选择策略，例如 surface format、present mode、swapchain extent 和 queue family 选择；实现与验证依据见阶段 7。
- [ ] 在后续 Render 扩展中补充 pipeline、descriptor、buffer/image resource 的分层边界。
- [ ] 结合 Linux 环境验证 SDL Vulkan surface、Volk 动态加载和 pthread/dl 链接行为。

**依赖关系**

- 依赖阶段 2 的 Vulkan-capable SDL 窗口。
- 依赖 `.clangd` 与 CMake 对 Volk/Vulkan include 和链接配置保持同步。

**验证依据**

- `cmake --build /e/RaythmDemo/build --config Debug` 构建通过。
- `ctest --test-dir /e/RaythmDemo/build -C Debug --output-on-failure` 执行通过，3/3 tests passed。
- `docs/开发日志.md` 中 `2026-07-04 建立 Render Vulkan 清屏与 Present 骨架`。

---

### 阶段 4：Platform 输入事件与事件泵演进

**状态**：`[x]` 已完成窗口事件泵、设备输入封装与输入状态系统

**目标**

- 在不引入过重消息总线的前提下，建立键盘、鼠标与窗口生命周期事件的统一采集边界。
- 将 SDL 全局事件队列消费职责从 `Window` 中剥离，避免窗口层与输入层争用事件队列。
- 为后续 Core 主循环、输入低延迟处理、输入录制回放和动作映射保留稳定扩展点。

**设计原则**

- 优先新增薄 `Platform::EventPump` 或同等职责类型，而不是新增“总消息处理类”。
- `EventPump` 只负责从 SDL 拉取事件并翻译为项目自有事件，不承载业务决策。
- `Window` 继续作为 SDL 窗口 RAII 封装，长期职责应收敛到窗口资源、窗口 ID、尺寸/显示状态和 Vulkan surface 创建。
- 输入模块应维护 `InputState` 或同等状态快照，而不是只做按键消息回调。
- 当前阶段由 `main` 或未来 `Core::Application` 显式消费事件；仅当多个模块确实需要订阅同一事件流时，再考虑正式 dispatcher/router。

**短期待办**

- [x] 定义轻量窗口事件封装并通过 `Platform::EventPump` 覆盖 close、resize、focus 等窗口生命周期事件。
- [x] 定义轻量输入事件类型，覆盖 keyboard、mouse button、mouse motion。
- [x] 新增 `EventPump` 测试，验证 SDL 窗口事件翻译、未知事件跳过和退出事件处理路径。
- [x] 将 `Window::pollEvent()` 中的 SDL 取事件逻辑迁移到唯一事件泵，并同步更新 `src/main.cpp` 调用方。
- [x] 新增输入状态系统测试，验证 key down/up、mouse button down/up、mouse motion 和帧边沿状态。
- [x] 在最小主循环中显式消费统一平台事件流；窗口事件已转发给渲染器，输入事件已接入 `InputState`，并在 focus lost 时清理 held 输入。

**验证依据**

- `cmake --build /e/RaythmDemo/build --config Debug` 构建通过。
- `ctest --test-dir /e/RaythmDemo/build -C Debug --output-on-failure` 执行通过，3/3 tests passed。
- `ctest --test-dir /e/RaythmDemo/build -C Debug -R Window --output-on-failure` 执行通过，1/1 tests passed。
- `tests/window_test.cpp` 已覆盖键盘、鼠标按键、鼠标移动、统一平台事件流、跨轮询路径事件保留与跨窗口事件保留。
- `tests/input_state_test.cpp` 已覆盖默认空状态、key down/up、repeat 抑制、同帧 press/release、mouse button down/up、mouse position/delta 与 clear reset。
- `RaythmDemoInputStateTest` 已在 `CMakeLists.txt` 中注册到 CTest。
- 已检查 `.clangd`，本次未改变 CMake 目标、include 路径、预处理宏或第三方依赖，无需同步修改。

---

### 阶段 5：Core 基础设施与应用生命周期收束

**状态**：`[x]` 已完成 Core 主循环、时间系统与应用生命周期基础收束

**目标**

- 建立 `Raythm::Core` 模块基础设施，提供 `Core::Application`、主循环、时间系统和统一应用生命周期管理。
- 将当前 `src/main.cpp` 中分散的窗口创建、事件泵、输入状态推进与渲染帧循环收束到 Core 层。
- 明确 Core 对 Platform、Render、Audio、Game 的统筹关系，为后续音频时钟同步、谱面推进、判定窗口和模块初始化顺序打底。

**设计原则**

- `Core::Application` 负责生命周期编排、模块初始化顺序、主循环驱动与退出流程，不直接承担 SDL/Vulkan 细节。
- `Platform` 继续负责窗口、事件泵、输入事件与输入状态；`Core` 消费项目自有事件和状态，不向上暴露 SDL 原始事件。
- `Render` 继续管理 Vulkan 资源与渲染帧提交；`Core` 只负责调用顺序、错误传播和退出协调。
- 时间系统优先提供 monotonic time、delta time、elapsed time 与 frame counter；先保持可变步长主循环，固定步长策略留到 Game/判定需求明确后再引入。
- 主循环先保持单线程、显式顺序执行：`beginFrame()`、拉取平台事件、更新输入状态、处理窗口事件、提交渲染、检查退出。
- 初始化和析构顺序必须可追踪，至少覆盖 SDL video 初始化、窗口创建、Renderer 初始化、主循环运行、Renderer idle、SDL shutdown。

**完成情况**

- [x] 新增 `include/Core/Application.hpp` 与 `src/Core/Application.cpp`，建立 `Raythm::Core::Application` 基础骨架。
- [x] 新增 `include/Core/Time.hpp` 与 `src/Core/Time.cpp`，封装 monotonic clock、delta time、elapsed time 与 frame counter。
- [x] 将 `src/main.cpp` 收缩为最小入口，仅负责创建 `Core::Application` 并调用 `run()`。
- [x] 在 `Application` 内接管 `Platform::Window`、`Platform::EventPump`、`Platform::InputState` 与 `Render::Renderer` 的创建、帧推进和退出流程。
- [x] 明确窗口关闭、`SDL_EVENT_QUIT`、focus lost、渲染失败、初始化失败等路径的退出语义与错误传播边界。
- [x] 为 Core 模块新增并注册 CTest，至少覆盖时间推进、frame counter、退出请求和最小生命周期流程。

**短期待办**

- [x] 先抽离 `src/main.cpp` 中现有最小循环到 `Core::Application::run()`，保持行为不变，优先完成职责收束。
- [x] 设计 `ApplicationOptions`、`ApplicationResult` 或同等轻量结构，集中表达窗口配置、退出结果和启动失败原因。
- [x] 为时间系统明确采样点：帧开始时间、上一帧 delta、累计运行时长和 frame index。
- [x] 固化 `InputState::beginFrame()`、事件消费、focus lost 清理、`Renderer::handleWindowEvent()` 与 `Renderer::renderFrame()` 的调用顺序。
- [x] 保持 Core 对 Platform/Render 的单向依赖，避免 Platform 或 Render 反向包含 Core。
- [x] 检查 Core 模块接入后 `CMakeLists.txt`、CTest 与 `.clangd` 是否需要同步更新。

**依赖关系**

- 依赖阶段 2 与阶段 4 已完成的 `Platform::Window`、`Platform::EventPump`、`Platform::InputState`。
- 依赖阶段 3 已完成的 `Render::Renderer` 最小清屏与 Present 骨架。
- 依赖当前 `src/main.cpp` 已验证的最小 SDL/Vulkan 主循环行为作为迁移基线。
- 后续 Audio、Game 模块将依赖本阶段输出的生命周期、时间推进和模块初始化边界。

**验证依据**

- `cmake --build /e/RaythmDemo/build --config Debug` 构建通过。
- `ctest --test-dir /e/RaythmDemo/build -C Debug --output-on-failure` 执行通过，5/5 tests passed。
- `RaythmDemoCoreTest` 覆盖时间系统初始帧、delta/elapsed/frame counter 推进、大 delta clamp、reset、预运行退出请求、Application 默认配置和退出状态。
- `docs/开发日志.md` 中 `2026-07-04 建立 Core 基础设施与应用生命周期收束` 记录 Core 基础设施实现与验证结果。
- 已检查 `.clangd`，既有 `include/`、vcpkg include 与平台宏覆盖新增 Core 文件和目标公共依赖，本次无需同步修改。

---

### 阶段 6：Audio 最小播放与音频时钟骨架

**状态**：`[x]` 已完成最小播放、状态机与 playback time 骨架

**目标**

- 在制作 Game 最小可玩切片前，先建立 `Raythm::Audio` 的最小可用播放骨架。
- 使用 miniaudio 建立 RAII 音频生命周期封装，提供音乐加载、播放、暂停、停止与关闭边界。
- 提供可信的音频播放时间 `playback time`，作为后续谱面滚动、判定窗口和音画同步的主时间基准。

**设计原则**

- 音游的主时钟应优先来自音频播放进度，而不是单纯使用渲染帧 delta。
- 当前阶段不做完整混音器、音效池、DSP、变速、音频延迟校准或高级低延迟调优。
- Audio 应通过明确状态暴露播放生命周期，例如 `Stopped`、`Playing`、`Paused`、`Ended`。
- Game 层后续应依赖 Audio 提供的 playback time 或可替代的 fake audio clock，避免直接采样系统时间。
- 测试应优先覆盖状态机、时间语义和无设备/无文件路径；真实音频设备 smoke test 可在不可用环境中跳过。

**完成情况**

- [x] 新增 `include/Audio/AudioEngine.hpp` 与 `src/Audio/AudioEngine.cpp` 或同等职责文件。
- [x] 新增 `Raythm::Audio` 命名空间下的音频设备/引擎 RAII 封装。
- [x] 支持加载一个本地音乐文件，并提供 `play()`、`pause()`、`stop()` 或同等控制接口。
- [x] 提供 `getPlaybackTimeSeconds()` 或同等播放时间查询接口。
- [x] 明确播放状态、结束状态、重复启动、暂停恢复、停止重置等语义。
- [x] 新增并注册 Audio CTest，覆盖状态机和 fake clock/无设备路径。

**短期待办**

- [x] 先设计 `AudioEngine` 的最小公开 API，避免 Game 后续直接绑定 miniaudio 细节。
- [x] 明确 playback time 在暂停、恢复、停止、seek 之后的语义。
- [x] 设计 fake audio clock 或测试替身，使 Game 判定逻辑可以脱离真实声卡测试。
- [x] 检查 `CMakeLists.txt`、`third_party/miniaudio/` include、CTest 与 `.clangd` 是否需要同步更新。

**依赖关系**

- 依赖阶段 1 已接入的 miniaudio 单头文件基础验证。
- 依赖阶段 5 的 Core 生命周期边界，后续由 Core 统筹 Audio 初始化和关闭顺序。
- 后续 Game 最小可玩切片应依赖本阶段输出的音频播放时间。

**验证依据**

- `cmake --build /e/RaythmDemo/build --config Debug` 构建通过。
- `ctest --test-dir /e/RaythmDemo/build -C Debug --output-on-failure` 执行通过，6/6 tests passed。
- `RaythmDemoAudioTest` 覆盖初始停止状态、fake clock 时间推进、暂停/恢复/停止、重复 play、Ended 状态、无曲目播放、无设备缺失文件路径、嵌入 NUL 路径和非法 fake track duration。
- Audio 测试覆盖播放状态、时间推进、暂停/恢复/停止语义和无设备/无文件容错路径。
- `docs/开发日志.md` 中记录 Audio 最小播放与音频时钟骨架实现与验证结果。

---

### 阶段 7：Render 纯策略测试与最小 2D 绘制能力

**状态**：`[x]` 已完成 Render 纯策略测试与最小 2D colored rect 绘制入口

**目标**

- 先补齐 Render 层中可脱离真实 GPU 的 swapchain 选择策略测试，降低 Vulkan 环境依赖。
- 将当前清屏 Present 骨架演进为可绘制最小 2D 元素的渲染入口。
- 为 Game 最小可玩切片提供轨道、判定线和 tap note 的基础绘制能力。

**设计原则**

- 不在当前阶段封装完整渲染引擎、资源系统、贴图、字体、粒子、后处理或复杂 UI。
- 优先拆出可测试的纯选择策略，例如 surface format、present mode、swapchain extent、queue family 选择。
- Game 层不应直接接触 Vulkan 对象；应通过 `drawRect`、`drawLine`、`RenderCommand` 或同等最小绘制接口表达 2D 绘制需求。
- 2D 能力先以 colored quad/line 为主，足够表达轨道、判定线、下落 note 和简单命中反馈。

**完成情况**

- [x] 拆分 surface format、present mode、swapchain extent、queue family 等纯策略函数或策略类型。
- [x] 新增并注册 Render 纯策略 CTest，覆盖无 GPU 环境下的选择逻辑。
- [x] 设计最小 2D 绘制接口，避免 Game 直接依赖 Vulkan command buffer 或 pipeline 细节。
- [x] 建立 colored rect 的最小 Vulkan render pass、framebuffer 与提交路径，轨道、判定线和 tap note 先以矩形表达。
- [x] 支持用屏幕坐标或正交投影绘制轨道、判定线和 tap note。

**短期待办**

- [x] 优先把现有 `Renderer` 中的 private swapchain 选择逻辑移动到可测试边界。
- [x] 再设计 `Renderer::beginFrame()` / `endFrame()` 或 `RenderCommand` 队列形态，给 Game 留提交绘制命令的入口。
- [x] 最小 2D pipeline 完成前，不引入贴图、字体、复杂材质和资源热加载。
- [ ] 继续评估 Vulkan fence/acquire 等待超时、device lost 与恢复策略。

**依赖关系**

- 依赖阶段 3 的 Vulkan 清屏 Present 骨架。
- 依赖阶段 5 的 Core 主循环，由 Core 驱动 Render 帧提交。
- 后续 Game 最小可玩切片依赖本阶段输出的轨道和 note 绘制能力。

**验证依据**

- `cmake --build /e/RaythmDemo/build --config Debug` 构建通过。
- `ctest --test-dir /e/RaythmDemo/build -C Debug --output-on-failure` 执行通过，7/7 tests passed。
- `RaythmDemoRenderStrategyTest` 无需真实 GPU 即可覆盖格式、present mode、extent、queue family 选择和矩形裁剪。
- `RaythmDemoRenderTest` 继续覆盖真实 SDL/Vulkan 清屏、Present、resize 和最小 2D 绘制路径。
- `docs/开发日志.md` 中 `2026-07-04 完成 Render 纯策略测试与最小 2D 绘制能力` 记录 Stage 7 实现与验证结果。

---

### 阶段 8：Game 最小可玩切片

**状态**：`[x]` 最小 Core 运行时游玩闭环已建立，真实 Vulkan-capable 环境手动游玩复测待完成

**目标**

- 在 Audio playback time 和 Render 2D 能力具备后，建立第一条音游最小可玩闭环。
- 优先实现 4K 下落式、tap note、固定判定线、键盘输入判定和 JSON 谱面雏形。
- 让一首最小测试谱面能够随音频时间推进、显示 note、响应输入并产出判定结果。

**设计原则**

- 第一版只做 tap note，不引入 hold、滑键、变速、特效、皮肤、编辑器或完整曲库系统。
- 判定、note 生命周期和谱面推进应优先用纯逻辑测试覆盖，不依赖真实窗口、GPU 或声卡。
- Game 推进应基于 Audio playback time；Core frame time 只作为帧驱动和辅助诊断信息。
- 输入从 `Platform::InputState` 经由语义输入映射层转换为 `LaneInputSnapshot` 后再进入判定逻辑，不直接依赖 SDL 原始事件或物理按键细节。
- Game 逻辑对外提供 `GameplaySnapshot` 或同等只读运行时快照，玩法视图适配层负责将其转换为 `RenderCommand`，避免 Render 承载谱面判定或业务逻辑。

**完成情况**

- [x] 新增 `include/Game/` 与 `src/Game/` 下的最小玩法模型。
- [x] 定义 `Chart`、`Note`、`LaneInputSnapshot`、`GameplaySnapshot`、`JudgementResult`、`JudgementEvent`、`JudgementCounts` 与 `GameplaySession` 等基础类型。
- [x] 实现谱面文件解析和加载，读取标准 JSON 谱面文件并将 `meta`、`time`、`note` 转换为运行时 `Chart`。
- [x] 新增 `.mc` 谱面转换器，将 Malody 风格的 `meta.song`、BPM 时间线、可游玩 note 和音频事件转换为 RaythmDemo 标准 JSON 谱面。
- [x] 定义 4K 默认输入映射，例如四个键盘按键对应四条轨道。
- [x] 建立语义输入映射层 `InputState -> LaneInputSnapshot`，隔离物理按键状态与轨道判定输入。
- [x] 建立判定窗口：Critical Perfect、Normal Perfect、Great、Bad、Miss，首版按 `docs/玩法设计文档/玩法定义.md` 的 tap 判定窗口实现。
- [x] 支持从 JSON 或临时测试数据构造最小谱面。
- [x] 用 Audio playback time 语义时间推进 note 生命周期、miss 和屏幕位置；当前通过纯 `GameplaySession::update(currentSongTime, LaneInputSnapshot)` 接口接收外部播放时间，真实 `AudioEngine` 接入留待运行时闭环。
- [x] 定义 `GameplaySnapshot` 或同等只读视图数据，表达轨道、判定线和可见 note。
- [x] 建立玩法视图适配层 `GameplaySnapshot -> RenderCommand`，通过 Render 最小 2D 接口绘制轨道、判定线和 tap note。
- [x] 新增并注册 Game CTest，覆盖谱面解析、输入映射、同帧边沿输入、渲染命令适配、tap 判定窗口、过早输入、自动 Miss、combo、offset、多 BPM、hold 头判、timestamped press event、非法 timestamped event batch、极端时间输入、非法 beat 数据与 `GameplaySnapshot` 更新；真实 Core/Audio/Render 运行闭环留待后续实现。
- [x] 新增 `Core::GameplayRuntimeDriver` 并接入 `Core::Application`，运行时可加载 staged `assets/charts/00001/00001.json` 与 Ogg 音频，用 Audio playback time、`InputState`、timestamped `LanePressEvent`、`GameplaySession` 和 `GameplayViewAdapter` 生成并提交 `RenderCommand`。
- [x] 为运行时音频资产添加可执行文件相对 asset root、CMake POST_BUILD staging、no-follow/open-then-verify 读取、Ogg signature 校验、大小限制和 memory-backed miniaudio decode，避免依赖 CWD 或校验后按路径重新打开。

**短期待办**

- [x] 先确定 4K 默认键位和 JSON 谱面最小字段；判定窗口初始数值留待判定闭环实现。
- [x] 先实现谱面文件解析和加载，使 `docs/玩法设计文档/谱面文件格式定义.md` 中的最小格式能进入 `Chart` 运行时模型。
- [x] 新增 `.mc` 到 RaythmDemo JSON 的离线转换器，并将转换器测试注册到 CTest。
- [x] 再建立 `InputState -> LaneInputSnapshot` 语义输入映射层，保证判定逻辑不依赖平台物理输入细节。
- [x] 再建立 `GameplaySnapshot -> RenderCommand` 玩法视图适配层，保证渲染层只消费 plain render commands。
- [x] 先写纯 Game 逻辑测试，再接入 Audio/Render 的真实运行路径。
- [x] 实现 `GameplaySession` 会话层，基于 `Chart`、Audio playback time 语义时间与 `LaneInputSnapshot` 推进 tap note 判定、miss、combo、判定计数和只读 `GameplaySnapshot`。
- [x] 为判定闭环补充 CTest 纯逻辑覆盖：Critical Perfect/Normal Perfect/Great/Bad/Miss 时间窗口、过早输入、重复命中防护、miss 后 combo 清空、多轨道相邻 note 互不干扰、timestamped press event、非法 timestamped event batch、极端时间输入、非法 beat 数据、offset、多 BPM 和 hold 头判。
- [x] 纯逻辑稳定后，再将 Core 最小运行时接入为：读取 Audio playback time、读取 `InputState`、更新 GameplaySession、生成 GameplaySnapshot、通过 GameplayViewAdapter 提交 RenderCommand。
- [x] 保持首个切片可玩、可测、可删改，不提前绑定最终美术、UI 或谱面编辑器设计。
- [x] 在 Vulkan-capable SDL 环境下复测真实窗口、音乐播放、note 滚动和 D/F/J/K 输入判定。
- [ ] 后续补齐暂停/恢复 UI、选曲入口、完整 score、hold sustain/release 和结果统计。

**依赖关系**

- 依赖阶段 4 的输入状态系统，Game 层通过 `InputState -> LaneInputSnapshot` 适配层消费语义轨道输入。
- 依赖阶段 5 的 Core 生命周期和时间系统。
- 依赖阶段 6 的 Audio playback time。
- 依赖阶段 7 的 Render 最小 2D 绘制能力。

**验证依据**

- `cmake --build /e/RaythmDemo/build --config Debug --target RaythmDemoGameTest` 构建通过，生成 `RaythmDemoGame` 与 `RaythmDemoGameTest`。
- `cmake --build build --config Debug --target RaythmDemoGameplaySessionTest` 构建通过，生成新增 `RaythmDemoGameplaySessionTest`。
- `python tools/mc_to_chart_json.py assets/charts/00001/1627802685.mc --stdout --pretty > /dev/null` 执行通过，真实 `.mc` 样例可转换为 RaythmDemo JSON。
- `python -m unittest tests/test_mc_to_chart_json.py -v` 执行通过，14/14 tests passed。
- `ctest --test-dir /e/RaythmDemo/build -C Debug -R Game --output-on-failure` 执行通过，1/1 tests passed。
- `ctest --test-dir build -C Debug --output-on-failure -R "Game|GameplaySession"` 执行通过，2/2 tests passed。
- `cmake --build /e/RaythmDemo/build --config Debug` 全量 Debug 构建通过。
- `ctest --test-dir /e/RaythmDemo/build -C Debug --output-on-failure` 全量 CTest 执行通过，11/11 tests passed。
- `RaythmDemoGameTest` 覆盖谱面 JSON 文本解析、谱面文件加载、beat 规约、BPM 排序与重复检测、column/endbeat 校验、默认 D/F/J/K 输入映射、同帧边沿输入、`GameplaySnapshot -> RenderCommand` 转换和 note 裁剪。
- `RaythmDemoGameplaySessionTest` 覆盖 `GameplaySession` tap 判定窗口、自动 Miss、combo、offset、多 BPM、hold 头判、timestamped press event、非法 timestamped event batch、极端时间输入、非法 beat 数据和 snapshot 更新。
- `RaythmDemoCoreGameplayRuntimeTest` 覆盖 Core 运行时 driver 的 baseline/note commands、默认键位判定、held lane 视觉、viewport resize、timestamped dense same-lane press events、bound key event 转换、非法 viewport 和 Miss sweep。
- `RaythmDemo` 构建时通过 CMake POST_BUILD 将 `assets/` staged 到 `build/Debug/assets`，供可执行文件相对 asset root 加载启动谱面与音频。
- `Core::Application` 主循环每帧在 `EventPump::pollEvent()` 前显式调用 `SDL_PumpEvents()`，解决 `SDL_PeepEvents()` 不隐式 pump 系统消息导致的 Windows 窗口未响应问题。
- `RaythmDemoEnvironmentTest` 覆盖项目 Ogg Vorbis 音频资产的 miniaudio decoder 初始化、格式查询和 PCM frame 读取。
- `RaythmDemoAudioTest` 覆盖 `AudioEngine::loadMusicFromMemory()` 对真实 `assets/charts/00001/00001.ogg` bytes 的加载和播放启动。
- `timeout 5s ./build/Debug/RaythmDemo.exe` 未再输出 `unable to load music track 'charts/00001/00001.ogg'`，程序保持运行至 timeout，说明启动 Ogg 音频加载失败已消除。
- 人工运行 `build/Debug/RaythmDemo.exe` 完成最小可玩切片验证：窗口打开后不再提示未响应，音乐与谱面同步播放，note 滚动与 D/F/J/K 输入交互符合当前最小切片预期。
- 已使用 `code-reviewer`、`cpp-reviewer` 与 `security-reviewer` 审查，并完成阻塞项修复与复审。
- 当前 shell 环境执行 `timeout 5s ./build/Debug/RaythmDemo.exe` 已验证启动 Ogg 音频加载失败不再复现；真实窗口、音频、note 滚动和 D/F/J/K 输入已在 Vulkan-capable SDL 环境完成最小可玩切片人工复测。
- 最小可玩切片自动化测试、启动 smoke 与真实运行验证已覆盖本次 seam 与运行时闭环；完整 score 公式、hold sustain/release 判定、Linux 验证和音频/视觉 offset 校准仍是后续任务。

---

## 维护规则

- 每次开始开发前，先阅读 `docs/开发日志.md` 和本文档，确认当前已完成事项、遗留问题和短期计划。
- 完成计划项后，将对应任务标记为 `[x]`，并在 `docs/开发日志.md` 中记录完成内容、验证结果和后续计划。
- 完成模块实现、测试接入、架构调整、重要 Bug 修复或阶段性验证后，必须同步更新本文档的计划总览、对应阶段状态、完成情况、短期待办、验证依据和版本历史。
- 如果实际代码状态与本文档不一致，以代码和验证结果为准，并同步修正本文档与开发日志。
- 新增计划应优先落到短期计划或对应阶段计划中，避免只写在对话里而没有文档追踪。

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 3.2 | 2026-07-07 | 完成最小可玩切片运行验证与窗口响应修复 |
| 3.1 | 2026-07-07 | 完成运行时卡死与音频/谱面不同步首轮修复 |
| 3.0 | 2026-07-07 | 补充运行时卡死与音频/谱面不同步修复方案 |
| 2.9 | 2026-07-07 | 修复 Ogg Vorbis 启动音频加载失败并更新验证依据 |
| 2.8 | 2026-07-07 | 完成最小 Core 运行时游玩闭环计划与验证依据更新 |
| 2.7 | 2026-07-07 | 补充下一步最小 Core 运行闭环推进计划 |
| 2.6 | 2026-07-07 | 完成 GameplaySession 纯逻辑 tap 判定闭环计划与验证依据更新 |
| 2.5 | 2026-07-06 | 补充当前进度摘要、下一步 Game 判定闭环建议，并修正 Render 阶段状态 |
| 2.4 | 2026-07-06 | 新增 .mc 谱面转换器计划与验证依据 |
| 2.3 | 2026-07-06 | 建立 Game 谱面加载、语义输入映射与玩法视图适配 seam |
| 2.2 | 2026-07-05 | 将谱面文件解析加载、语义输入映射层和玩法视图适配层纳入 Game 最小可玩切片计划 |
| 2.1 | 2026-07-04 | 完成 Render 纯策略测试与最小 2D 绘制能力 |
| 2.0 | 2026-07-04 | 完成 Audio 最小播放与音频时钟骨架 |
| 1.9 | 2026-07-04 | 新增 Audio 最小播放、Render 最小 2D 与 Game 最小可玩切片阶段计划 |
| 1.8 | 2026-07-04 | 完成 Core 基础设施、应用生命周期收束与 Core 测试接入 |
| 1.7 | 2026-07-04 | 设置 Core 基础设施与应用生命周期收束阶段计划 |
| 1.6 | 2026-07-04 | 补充下一步 Core 主循环、时间系统、模块初始化顺序、Render 策略测试与 Linux 验证计划 |
| 1.5 | 2026-07-04 | 更新 Platform 输入状态系统完成状态 |
| 1.4 | 2026-07-04 | 更新 Platform 设备输入事件封装完成状态 |
| 1.3 | 2026-07-04 | 更新 Platform 事件泵窗口事件封装完成状态 |
| 1.2 | 2026-07-04 | 补充 Platform 输入事件与事件泵演进计划 |
| 1.1 | 2026-07-04 | 更新 Render 清屏 Present 骨架完成状态并补充开发计划维护规则 |
| 1.0 | 2026-07-04 | 根据开发日志创建开发计划，梳理已完成计划和短期计划 |
