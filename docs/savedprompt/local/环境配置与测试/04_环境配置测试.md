按照以下方案进行开发环境测试：

1. 校验目前开发环境是否配置完整
2. 新建 src/miniaudio_impl.cpp 用于编译 miniaudio 实现
3. 新建 src/main.cpp ，编写开发环境的测试代码，测试对象包括 miniaudio，Vulkan/Volk，SDL3，nlohmann/json
4. 更新 CMakeLists.txt ，把上面的文件加入编译列表


CMakeLists.txt 给 MSVC 加上 /utf-8


 main.cpp 在执行到 VkResult result = volkInitialize(); 时崩溃，请分析原因，并给出合理的修复方案


继续补充.gitignore，将 build-review 容纳进去


编辑器报错 'miniaudio.h' file not found ，但是编译通过，请分析原因并提供解决方案


把目前 main.cpp 的代码迁移到 tests 目录下
